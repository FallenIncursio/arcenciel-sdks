/* tslint:disable */
/* eslint-disable */
/**
 * Arc en Ciel Developer API
 * Stable, SDK-oriented Arc en Ciel API v1 contract for catalogue discovery, personal and social workflows, creator publishing, generation, private chat, feedback, privacy-filtered trust and safety cases, managed media, collections, and safe binary transfers. The complete public API reference contains additional evolving operations outside this compatibility profile.
 *
 * The version of the OpenAPI document: 1.8.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ApiError,
    ApiErrorFromJSON,
    ApiErrorToJSON,
} from '../models/ApiError.js';
import {
    type DeveloperFeedback,
    DeveloperFeedbackFromJSON,
    DeveloperFeedbackToJSON,
} from '../models/DeveloperFeedback.js';
import {
    type DeveloperFeedbackDeleteResponse,
    DeveloperFeedbackDeleteResponseFromJSON,
    DeveloperFeedbackDeleteResponseToJSON,
} from '../models/DeveloperFeedbackDeleteResponse.js';

export interface CreateFeedbackRequest {
    category: string;
    description: string;
    title: string;
    idempotencyKey?: string;
    attachments?: Array<Blob>;
    contactDiscord?: string;
    source?: string;
}

export interface DeleteFeedbackRequest {
    publicId: string;
}

export interface DownloadFeedbackAttachmentRequest {
    publicId: string;
    attachmentId: string;
    download?: DownloadFeedbackAttachmentDownloadEnum;
}

export interface GetFeedbackRequest {
    publicId: string;
}

/**
 *
 */
export class FeedbackApi extends runtime.BaseAPI {

    /**
     * Creates request options for createFeedback without sending the request
     */
    async createFeedbackRequestOpts(requestParameters: CreateFeedbackRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['category'] == null) {
            throw new runtime.RequiredError(
                'category',
                'Required parameter "category" was null or undefined when calling createFeedback().'
            );
        }

        if (requestParameters['description'] == null) {
            throw new runtime.RequiredError(
                'description',
                'Required parameter "description" was null or undefined when calling createFeedback().'
            );
        }

        if (requestParameters['title'] == null) {
            throw new runtime.RequiredError(
                'title',
                'Required parameter "title" was null or undefined when calling createFeedback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['attachments'] != null) {
            requestParameters['attachments'].forEach((element) => {
                formParams.append('attachments', element as any);
            })
        }

        if (requestParameters['category'] != null) {
            formParams.append('category', requestParameters['category'] as any);
        }

        if (requestParameters['contactDiscord'] != null) {
            formParams.append('contactDiscord', requestParameters['contactDiscord'] as any);
        }

        if (requestParameters['description'] != null) {
            formParams.append('description', requestParameters['description'] as any);
        }

        if (requestParameters['source'] != null) {
            formParams.append('source', requestParameters['source'] as any);
        }

        if (requestParameters['title'] != null) {
            formParams.append('title', requestParameters['title'] as any);
        }


        let urlPath = `/api/feedback`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Submit authenticated product feedback with up to six validated attachments. Reuse an Idempotency-Key and identical multipart payload when retrying an ambiguous request.
     * Submit a feedback item
     */
    async createFeedbackRaw(requestParameters: CreateFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeveloperFeedback>> {
        const requestOptions = await this.createFeedbackRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeveloperFeedbackFromJSON(jsonValue));
    }

    /**
     * Submit authenticated product feedback with up to six validated attachments. Reuse an Idempotency-Key and identical multipart payload when retrying an ambiguous request.
     * Submit a feedback item
     */
    async createFeedback(requestParameters: CreateFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeveloperFeedback> {
        const response = await this.createFeedbackRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteFeedback without sending the request
     */
    async deleteFeedbackRequestOpts(requestParameters: DeleteFeedbackRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['publicId'] == null) {
            throw new runtime.RequiredError(
                'publicId',
                'Required parameter "publicId" was null or undefined when calling deleteFeedback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/feedback/{publicId}`;
        urlPath = urlPath.replace('{publicId}', encodeURIComponent(String(requestParameters['publicId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Delete a feedback case owned by the authenticated caller, including its stored attachment files. Staff may delete a case through the same audited operation.
     * Delete one feedback item
     */
    async deleteFeedbackRaw(requestParameters: DeleteFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeveloperFeedbackDeleteResponse>> {
        const requestOptions = await this.deleteFeedbackRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeveloperFeedbackDeleteResponseFromJSON(jsonValue));
    }

    /**
     * Delete a feedback case owned by the authenticated caller, including its stored attachment files. Staff may delete a case through the same audited operation.
     * Delete one feedback item
     */
    async deleteFeedback(requestParameters: DeleteFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeveloperFeedbackDeleteResponse> {
        const response = await this.deleteFeedbackRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadFeedbackAttachment without sending the request
     */
    async downloadFeedbackAttachmentRequestOpts(requestParameters: DownloadFeedbackAttachmentRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['publicId'] == null) {
            throw new runtime.RequiredError(
                'publicId',
                'Required parameter "publicId" was null or undefined when calling downloadFeedbackAttachment().'
            );
        }

        if (requestParameters['attachmentId'] == null) {
            throw new runtime.RequiredError(
                'attachmentId',
                'Required parameter "attachmentId" was null or undefined when calling downloadFeedbackAttachment().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['download'] != null) {
            queryParameters['download'] = requestParameters['download'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/feedback/{publicId}/attachments/{attachmentId}`;
        urlPath = urlPath.replace('{publicId}', encodeURIComponent(String(requestParameters['publicId'])));
        urlPath = urlPath.replace('{attachmentId}', encodeURIComponent(String(requestParameters['attachmentId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Stream a non-expired feedback attachment to the original reporter or staff. Honor the returned media type and Content-Disposition filename; attachment URLs are private.
     * View or download one feedback attachment
     */
    async downloadFeedbackAttachmentRaw(requestParameters: DownloadFeedbackAttachmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.downloadFeedbackAttachmentRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Stream a non-expired feedback attachment to the original reporter or staff. Honor the returned media type and Content-Disposition filename; attachment URLs are private.
     * View or download one feedback attachment
     */
    async downloadFeedbackAttachment(requestParameters: DownloadFeedbackAttachmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.downloadFeedbackAttachmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFeedback without sending the request
     */
    async getFeedbackRequestOpts(requestParameters: GetFeedbackRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['publicId'] == null) {
            throw new runtime.RequiredError(
                'publicId',
                'Required parameter "publicId" was null or undefined when calling getFeedback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/feedback/{publicId}`;
        urlPath = urlPath.replace('{publicId}', encodeURIComponent(String(requestParameters['publicId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return one feedback case to its reporter or staff. Reporter contact data is case-bound and internal moderation notes remain null for ordinary callers.
     * Get one feedback item
     */
    async getFeedbackRaw(requestParameters: GetFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeveloperFeedback>> {
        const requestOptions = await this.getFeedbackRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeveloperFeedbackFromJSON(jsonValue));
    }

    /**
     * Return one feedback case to its reporter or staff. Reporter contact data is case-bound and internal moderation notes remain null for ordinary callers.
     * Get one feedback item
     */
    async getFeedback(requestParameters: GetFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeveloperFeedback> {
        const response = await this.getFeedbackRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listMyFeedback without sending the request
     */
    async listMyFeedbackRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["x-api-key"] = await this.configuration.apiKey("x-api-key"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/feedback/me`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List up to 100 feedback cases submitted by the authenticated caller. Other users’ cases and staff-only internal notes are never returned.
     * List feedback submitted by the current user
     */
    async listMyFeedbackRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DeveloperFeedback>>> {
        const requestOptions = await this.listMyFeedbackRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DeveloperFeedbackFromJSON));
    }

    /**
     * List up to 100 feedback cases submitted by the authenticated caller. Other users’ cases and staff-only internal notes are never returned.
     * List feedback submitted by the current user
     */
    async listMyFeedback(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DeveloperFeedback>> {
        const response = await this.listMyFeedbackRaw(initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const DownloadFeedbackAttachmentDownloadEnum = {
    _1: '1',
    True: 'true',
    Yes: 'yes',
    UnknownDefaultOpenApi: '11184809'
} as const;
export type DownloadFeedbackAttachmentDownloadEnum = typeof DownloadFeedbackAttachmentDownloadEnum[keyof typeof DownloadFeedbackAttachmentDownloadEnum];
