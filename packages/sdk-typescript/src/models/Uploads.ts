/* tslint:disable */
/* eslint-disable */
/**
 * Arc en Ciel Developer API
 * Stable, SDK-oriented Arc en Ciel API v1 contract for catalogue discovery, personal and social workflows, creator publishing, generation, private chat, feedback, privacy-filtered trust and safety cases, managed media, collections, and safe binary transfers. The complete public API reference contains additional evolving operations outside this compatibility profile.  Runtime resource URLs deliberately remain under `/api/...`; v1 is versioned by this immutable contract rather than a `/api/v1/...` path prefix. Additive releases preserve existing v1 requests and responses, while a future breaking v2 will use a parallel migration surface and the published deprecation policy.
 *
 * The version of the OpenAPI document: 1.9.2
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { ApiKey } from './ApiKey.js';
import {
    ApiKeyFromJSON,
    ApiKeyFromJSONTyped,
    ApiKeyToJSON,
    ApiKeyToJSONTyped,
} from './ApiKey.js';
import type { User } from './User.js';
import {
    UserFromJSON,
    UserFromJSONTyped,
    UserToJSON,
    UserToJSONTyped,
} from './User.js';

/**
 * Structured uploads details for this self profile.
 * @export
 * @interface Uploads
 */
export interface Uploads {
    /**
     * Ordered articles entries included with this uploads; an empty list means none are available.
     * @type {Array<ApiKey>}
     * @memberof Uploads
     */
    articles?: Array<ApiKey>;
    /**
     * Ordered images entries included with this uploads; an empty list means none are available.
     * @type {Array<ApiKey>}
     * @memberof Uploads
     */
    images?: Array<ApiKey>;
    /**
     * Ordered models entries included with this uploads; an empty list means none are available.
     * @type {Array<User>}
     * @memberof Uploads
     */
    models?: Array<User>;
    /**
     * Ordered videos entries included with this uploads; an empty list means none are available.
     * @type {Array<ApiKey>}
     * @memberof Uploads
     */
    videos?: Array<ApiKey>;
}

/**
 * Check if a given object implements the Uploads interface.
 */
export function instanceOfUploads(value: object): value is Uploads {
    return true;
}

export function UploadsFromJSON(json: any): Uploads {
    return UploadsFromJSONTyped(json, false);
}

export function UploadsFromJSONTyped(json: any, ignoreDiscriminator: boolean): Uploads {
    if (json == null) {
        return json;
    }
    return {

        'articles': json['articles'] == null ? undefined : ((json['articles'] as Array<any>).map(ApiKeyFromJSON)),
        'images': json['images'] == null ? undefined : ((json['images'] as Array<any>).map(ApiKeyFromJSON)),
        'models': json['models'] == null ? undefined : ((json['models'] as Array<any>).map(UserFromJSON)),
        'videos': json['videos'] == null ? undefined : ((json['videos'] as Array<any>).map(ApiKeyFromJSON)),
    };
}

export function UploadsToJSON(json: any): Uploads {
    return UploadsToJSONTyped(json, false);
}

export function UploadsToJSONTyped(value?: Uploads | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {

        'articles': value['articles'] == null ? undefined : ((value['articles'] as Array<any>).map(ApiKeyToJSON)),
        'images': value['images'] == null ? undefined : ((value['images'] as Array<any>).map(ApiKeyToJSON)),
        'models': value['models'] == null ? undefined : ((value['models'] as Array<any>).map(UserToJSON)),
        'videos': value['videos'] == null ? undefined : ((value['videos'] as Array<any>).map(ApiKeyToJSON)),
    };
}
